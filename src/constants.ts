export enum Inputs {
    TagName = 'tag_name',
    Latest = 'latest'
}

export enum Outputs {
    ID = 'id',
    Url = 'url',
    HtmlUrl = 'html_url',
    AssetsUrl = 'assets_url',
    UploadUrl = 'upload_url',
    TagName = 'tag_name',
    Name = 'name',
    Body = 'body',
    Draft = 'draft',
    PreRelease = 'prerelease'
}
